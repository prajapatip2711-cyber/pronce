"use client";
import Link from "next/link";
import { useState } from "react";
import { FiMenu, FiSearch, FiShoppingBag, FiX } from "react-icons/fi";
import { useCart } from "./cart-provider";
const links=[['Home','/'],['Products','/products'],['Websites','/websites'],['About','/about'],['Contact','/contact'],['Track order','/track-order']];
export function Header(){const [open,setOpen]=useState(false); const {count}=useCart(); return <header className="sticky top-0 z-50 border-b border-white/10 bg-[#07070a]/75 backdrop-blur-xl"><nav className="mx-auto flex h-[70px] max-w-7xl items-center justify-between px-5"><Link href="/" className="font-black tracking-[-.08em] text-2xl">PRONCE<span className="text-lime">.</span></Link><div className="hidden items-center gap-6 md:flex">{links.map(([n,h])=><Link className="text-xs font-semibold text-white/65 hover:text-white" href={h} key={h}>{n}</Link>)}</div><div className="flex items-center gap-3"><Link href="/products" aria-label="Search" className="hidden text-white/70 sm:block"><FiSearch/></Link><Link href="/checkout" aria-label="Cart" className="relative rounded-full border border-white/15 p-2"><FiShoppingBag/>{count>0&&<span className="absolute -right-1 -top-1 grid h-4 w-4 place-items-center rounded-full bg-lime text-[9px] font-bold text-black">{count}</span>}</Link><button aria-label="Toggle menu" onClick={()=>setOpen(!open)} className="rounded-full border border-white/15 p-2 md:hidden">{open?<FiX/>:<FiMenu/>}</button></div></nav>{open&&<div className="border-t border-white/10 bg-[#0c0c10] px-5 py-5 md:hidden">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} className="block border-b border-white/10 py-3 text-sm font-bold" href={h} key={h}>{n}</Link>)}</div>}</header>}
