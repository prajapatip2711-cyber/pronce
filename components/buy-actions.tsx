"use client"; import Link from "next/link"; import { FiMessageCircle, FiShoppingBag } from "react-icons/fi"; import type { Product } from "@/lib/data"; import { whatsapp } from "@/lib/utils"; import { useCart } from "./cart-provider";
export function BuyActions({product}:{product:Product}){const {add}=useCart();return <div className="mt-8 flex flex-wrap gap-3"><button onClick={()=>add(product)} className="button-primary"><FiShoppingBag/> Add to cart</button><Link onClick={()=>add(product)} href="/checkout" className="button-secondary">Buy now</Link><a className="button-secondary" target="_blank" href={whatsapp(`Hi Prince, I'm interested in ${product.name}.`)}><FiMessageCircle/> WhatsApp</a></div>}
