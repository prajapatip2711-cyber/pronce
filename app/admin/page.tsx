"use client"; import { useState } from "react"; import { FiLock, FiPackage, FiUsers } from "react-icons/fi";
export default function Admin(){const [password,setPassword]=useState("");const [auth,setAuth]=useState(false);const [error,setError]=useState("");const login=async()=>{const r=await fetch('/api/admin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});if(r.ok)setAuth(true);else setError('Incorrect password.');};if(!auth)return <section className="mx-auto max-w-sm px-5 py-24"><div className="rounded-3xl border border-white/10 bg-white/[.03] p-7"><FiLock className="text-2xl text-lime"/><h1 className="mt-4 text-2xl font-bold">Admin access</h1><p className="mt-2 text-sm text-white/50">Sign in to manage your store data.</p><input className="field mt-6" type="password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==='Enter'&&login()} placeholder="Admin password"/><button className="button-primary mt-4 w-full" onClick={login}>Sign in</button>{error&&<p className="mt-3 text-sm text-red-300">{error}</p>}</div></section>;return <section className="mx-auto max-w-7xl px-5 py-16"><p className="eyebrow">Admin dashboard</p><h1 className="mt-3 text-4xl font-extrabold tracking-[-.06em]">Store control.</h1><div className="mt-10 grid gap-5 sm:grid-cols-3">{[[FiPackage,"Products","Manage catalog, stock and offers"],[FiUsers,"Customers","Review customer and order records"],[FiPackage,"Orders","Update fulfilment status"]].map(([Icon,title,text])=>{const I=Icon as typeof FiPackage;return <div key={title as string} className="rounded-2xl border border-white/10 p-6"><I className="text-xl text-lime"/><p className="mt-4 font-bold">{title as string}</p><p className="mt-2 text-sm text-white/50">{text as string}</p></div>})}</div><p className="mt-10 rounded-xl border border-lime/30 bg-lime/5 p-4 text-sm text-white/70">Use the connected Google Sheet as the source of truth. New orders and enquiries arrive in real time in their respective tabs.</p></section>}
