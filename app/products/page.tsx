"use client"; import { useMemo, useState } from "react"; import { products, categories } from "@/lib/data"; import { ProductCard } from "@/components/product-card";
export default function Products(){const [query,setQuery]=useState("");const [category,setCategory]=useState("All");const items=useMemo(()=>products.filter(p=>(category==="All"||p.category===category)&&p.name.toLowerCase().includes(query.toLowerCase())),[query,category]);return <section className="mx-auto max-w-7xl px-5 py-16"><p className="eyebrow">Physical goods</p><h1 className="mt-3 text-5xl font-extrabold tracking-[-.07em]">The good stuff.</h1><p className="mt-4 max-w-xl text-sm leading-6 text-white/55">Small luxuries and useful essentials, chosen for their form, feel and longevity.</p><div className="mt-10 flex flex-col gap-4 sm:flex-row"><input className="field sm:max-w-sm" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search the collection"/><select aria-label="Product category" className="field sm:max-w-[190px]" value={category} onChange={e=>setCategory(e.target.value)}><option>All</option>{categories.map(x=><option key={x}>{x}</option>)}</select></div><p className="mt-8 text-xs text-white/45">{items.length} product{items.length!==1?'s':''} available</p><div className="mt-4 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map(p=><ProductCard product={p} key={p.id}/>)}</div></section>}
