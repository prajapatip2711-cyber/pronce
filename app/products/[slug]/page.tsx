import Image from "next/image"; import { notFound } from "next/navigation"; import { FiCheck, FiStar } from "react-icons/fi"; import { products } from "@/lib/data"; import { money } from "@/lib/utils"; import { BuyActions } from "@/components/buy-actions"; import { ProductCard } from "@/components/product-card";
export default async function ProductDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=products.find(p=>p.slug===slug);if(!product)return notFound();return <><section className="mx-auto grid max-w-7xl gap-10 px-5 py-14 lg:grid-cols-2"><div className="relative aspect-square overflow-hidden rounded-[2rem] bg-white/5"><Image src={product.image} alt={product.name} fill priority sizes="(max-width:1024px) 100vw,50vw" className="object-cover"/></div><div className="py-3"><p className="eyebrow">{product.category}</p><h1 className="mt-3 text-4xl font-extrabold tracking-[-.065em] sm:text-6xl">{product.name}</h1><div className="mt-5 flex items-center gap-4"><span className="text-2xl font-bold">{money(product.price)}</span>{product.oldPrice&&<span className="text-white/35 line-through">{money(product.oldPrice)}</span>}<span className="flex items-center gap-1 text-sm text-white/60"><FiStar className="fill-lime text-lime"/>{product.rating} rating</span></div><p className="mt-7 max-w-xl text-base leading-7 text-white/60">{product.description}</p><div className="mt-7 grid gap-3 sm:grid-cols-2">{product.features.map(f=><p key={f} className="flex items-center gap-2 text-sm"><FiCheck className="text-lime"/>{f}</p>)}</div><BuyActions product={product}/><p className="mt-6 text-xs text-white/40">{product.stock} pieces available · GST calculated at checkout</p></div></section><section className="border-t border-white/10"><div className="mx-auto max-w-7xl px-5 py-16"><p className="eyebrow">Keep exploring</p><h2 className="mt-2 text-3xl font-extrabold tracking-[-.05em]">Related products</h2><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.filter(p=>p.id!==product.id).slice(0,3).map(p=><ProductCard product={p} key={p.id}/>)}</div></div></section></>}
