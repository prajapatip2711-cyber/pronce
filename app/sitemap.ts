import type { MetadataRoute } from "next"; import { products } from "@/lib/data"; import { siteUrl } from "@/lib/utils"; export default function sitemap():MetadataRoute.Sitemap{const paths=["","/products","/websites","/about","/contact","/track-order","/privacy","/terms","/returns","/shipping"];return [...paths.map(url=>({url:`${siteUrl}${url}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:url===""?1:.7})),...products.map(p=>({url:`${siteUrl}/products/${p.slug}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:.8}))]}
