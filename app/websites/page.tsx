import { FiArrowUpRight, FiGithub, FiMessageCircle } from "react-icons/fi"; import { websites } from "@/lib/data"; import { whatsapp } from "@/lib/utils";
export default function Websites(){return <section className="mx-auto max-w-7xl px-5 py-16"><p className="eyebrow">Digital studio</p><h1 className="mt-3 max-w-3xl text-5xl font-extrabold tracking-[-.075em] sm:text-7xl">Websites with a point of view.</h1><p className="mt-5 max-w-xl text-base leading-7 text-white/55">Strategic, expressive and designed to convert. Every project begins with the feeling it should leave behind.</p><div className="mt-12 grid gap-7 md:grid-cols-2">{websites.map(w=><article className="overflow-hidden rounded-3xl border border-white/10 bg-white/[.03]" key={w.title}><div className="h-72 bg-cover bg-center" style={{backgroundImage:`linear-gradient(0deg,rgba(7,7,10,.45),transparent),url(${w.image})`}}/><div className="p-6"><div className="flex items-center justify-between"><p className="eyebrow">{w.category}</p><FiArrowUpRight className="text-lime"/></div><h2 className="mt-3 text-2xl font-bold">{w.title}</h2><p className="mt-3 text-sm leading-6 text-white/55">{w.text}</p><p className="mt-5 font-mono text-[11px] text-white/40">{w.stack}</p><div className="mt-6 flex flex-wrap gap-3"><a href={whatsapp(`Hi Prince, I want a website similar to ${w.title}.`)} target="_blank" className="button-primary"><FiMessageCircle/> Order similar</a><a href="https://github.com" target="_blank" className="button-secondary" aria-label="GitHub"><FiGithub/></a></div></div></article>)}</div></section>}
