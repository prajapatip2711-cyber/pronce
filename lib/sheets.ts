export async function sendToSheets(sheet:string,payload:unknown){const url=process.env.GOOGLE_APPS_SCRIPT_URL;if(!url) throw new Error("GOOGLE_APPS_SCRIPT_URL is not configured");const response=await fetch(url,{method:"POST",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify({sheet,payload,source:"pronce-web",createdAt:new Date().toISOString()}),cache:"no-store"});if(!response.ok)throw new Error("Apps Script rejected request");return response;}
